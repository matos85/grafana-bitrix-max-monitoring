# Скопируйте в .github-credentials (файл в .gitignore, в GitHub не попадёт):
#   cp .github-credentials.example .github-credentials
#
# GitHub больше не принимает пароль аккаунта для git push.
# Создайте Personal Access Token: https://github.com/settings/tokens
# Права: repo (или fine-grained — Contents: Read and write)
#
GITHUB_USER=matos85
GITHUB_TOKEN=ghp_ВАШ_ТОКЕН
